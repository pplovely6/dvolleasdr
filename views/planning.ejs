<!DOCTYPE html>
<html lang="fr">

<head>
    <%- include('partials/head', { title: 'Planning Général | AS Dardilly' }) %>
        <style>
            .planning-hero {
                height: 40vh;
                background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(15, 17, 21, 1)), url('/m18-photo-garcon2025.jpg');
                display: flex;
                align-items: flex-end;
                padding-bottom: 40px;
                padding-left: 5%;
                background-size: cover;
                background-position: center;
            }

            .hero-title {
                font-family: var(--font-head);
                font-weight: 800;
                font-size: 5rem;
                text-transform: uppercase;
                line-height: 1;
                margin: 0;
                letter-spacing: -1px;
            }

            .hero-sub {
                color: #bbb;
                letter-spacing: 2px;
                font-weight: 600;
                margin-top: 10px;
                text-transform: uppercase;
                font-size: 1.1rem;
            }

            .planning-container {
                width: 90%;
                max-width: 1200px;
                margin: 0 auto;
                padding: 60px 0;
            }

            .filter-bar {
                display: flex;
                gap: 15px;
                margin-bottom: 40px;
                flex-wrap: wrap;
                justify-content: center;
            }

            .filter-btn {
                background: transparent;
                border: 1px solid #444;
                color: #aaa;
                padding: 12px 30px;
                font-family: var(--font-head);
                font-weight: 700;
                text-transform: uppercase;
                font-size: 0.95rem;
                letter-spacing: 1px;
                cursor: pointer;
                transition: 0.2s;
                border-radius: 4px;
            }

            .filter-btn:hover,
            .filter-btn.active {
                background: var(--brand-yellow);
                color: black;
                border-color: var(--brand-yellow);
            }

            .schedule-card {
                background: var(--bg-card);
                border: 1px solid var(--border);
                margin-bottom: 20px;
                padding: 25px;
                display: grid;
                grid-template-columns: 100px 1fr 150px 1fr;
                align-items: center;
                transition: transform 0.2s, border-color 0.2s;
                position: relative;
            }

            .schedule-card:hover {
                transform: translateY(-3px);
                border-color: #666;
            }

            .schedule-card::before {
                content: '';
                position: absolute;
                left: 0;
                top: 0;
                bottom: 0;
                width: 4px;
                background: var(--brand-yellow);
            }

            .date-col {
                text-align: center;
                border-right: 1px solid var(--border);
                padding-right: 20px;
                display: flex;
                flex-direction: column;
                justify-content: center;
            }

            .date-val {
                font-family: var(--font-head);
                font-weight: 800;
                font-size: 1.8rem;
                line-height: 1;
                color: white;
            }

            .date-month {
                font-size: 0.9rem;
                color: var(--brand-yellow);
                text-transform: uppercase;
                font-weight: 700;
            }

            .match-cat {
                font-size: 0.75rem;
                color: #888;
                text-transform: uppercase;
                margin-top: 5px;
                font-weight: 600;
            }

            .team-col {
                display: flex;
                align-items: center;
                gap: 15px;
                font-weight: 700;
                font-size: 1.2rem;
                text-transform: uppercase;
                font-family: var(--font-head);
            }

            .team-col.home {
                justify-content: flex-end;
                text-align: right;
            }

            .team-col.away {
                justify-content: flex-start;
                text-align: left;
            }

            .team-logo {
                height: 50px;
                width: auto;
                object-fit: contain;
            }

            .logo-placeholder {
                height: 45px;
                width: 45px;
                background: #252525;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 0.9rem;
                color: #666;
                font-weight: 700;
                border: 1px solid #333;
            }

            .info-col {
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                text-align: center;
            }

            .vs-badge {
                background: #333;
                color: white;
                font-weight: 800;
                padding: 4px 10px;
                border-radius: 4px;
                font-size: 0.9rem;
                margin-bottom: 5px;
            }

            .match-time {
                font-size: 1.1rem;
                font-weight: 700;
                color: var(--brand-yellow);
            }

            .match-loc {
                font-size: 0.8rem;
                color: #888;
                margin-top: 5px;
            }

            .empty-state {
                text-align: center;
                padding: 80px;
                border: 1px dashed var(--border);
                color: #666;
                font-size: 1.2rem;
                text-transform: uppercase;
                letter-spacing: 1px;
                font-weight: 600;
            }

            @media (max-width: 900px) {
                .hero-title {
                    font-size: 3rem;
                }

                .schedule-card {
                    grid-template-columns: 1fr;
                    gap: 15px;
                    text-align: center;
                    padding: 20px;
                }

                .schedule-card::before {
                    width: 100%;
                    height: 4px;
                    bottom: auto;
                    left: 0;
                }

                .date-col {
                    border-right: none;
                    border-bottom: 1px solid #333;
                    padding-right: 0;
                    padding-bottom: 15px;
                    flex-direction: row;
                    align-items: baseline;
                    gap: 10px;
                }

                .team-col {
                    justify-content: center !important;
                    text-align: center !important;
                    flex-direction: column;
                }

                .info-col {
                    margin: 10px 0;
                    background: #151515;
                    padding: 10px;
                    border-radius: 8px;
                }
            }
        </style>
</head>

<body>
    <%- include('partials/header') %>

        <div class="planning-hero">
            <div>
                <h1 class="hero-title">PLANNING <span style="color:var(--brand-yellow);">DU CLUB</span></h1>
                <p class="hero-sub" data-i18n="planning_title">TOUS LES MATCHS À VENIR</p>
            </div>
        </div>

        <main class="planning-container">
            <div class="filter-bar">
                <button class="filter-btn active" onclick="filterMatches('all')">Tout</button>
                <button class="filter-btn" onclick="filterMatches('dep')">DEP</button>
                <button class="filter-btn" onclick="filterMatches('m18')">M18</button>
                <button class="filter-btn" onclick="filterMatches('m15')">M15</button>
                <button class="filter-btn" onclick="filterMatches('m13')">M13</button>
            </div>

            <div id="full-schedule-list">
                <% if (allMatches.length===0) { %>
                    <div class="empty-state">Aucun match programmé.</div>
                    <% } else { %>
                        <% allMatches.forEach(m=> {
                            const dateParts = m.date.split(" ");
                            const isHome = m.home.toUpperCase().includes("DARDILLY");
                            %>
                            <div class="schedule-card" data-tag="<%= m.tag %>">
                                <div class="date-col">
                                    <div class="date-val">
                                        <%= dateParts[0] || "--" %>
                                    </div>
                                    <div class="date-month">
                                        <%= dateParts[1] || "" %>
                                    </div>
                                    <div class="match-cat">
                                        <%= m.category %>
                                    </div>
                                </div>

                                <div class="team-col home">
                                    <span>
                                        <%= m.home %>
                                    </span>
                                    <% if (isHome) { %>
                                        <img src="/image_fd6867.png" class="team-logo">
                                        <% } else { %>
                                            <div class="logo-placeholder">
                                                <%= m.home.substring(0,3).toUpperCase() %>
                                            </div>
                                            <% } %>
                                </div>

                                <div class="info-col">
                                    <div class="vs-badge">VS</div>
                                    <div class="match-time">
                                        <%= m.time %>
                                    </div>
                                    <div class="match-loc"><i class="fa-solid fa-location-dot"></i>
                                        <%= m.location %>
                                    </div>
                                </div>

                                <div class="team-col away">
                                    <% if (!isHome) { %>
                                        <img src="/image_fd6867.png" class="team-logo">
                                        <% } else { %>
                                            <div class="logo-placeholder">
                                                <%= m.away.substring(0,3).toUpperCase() %>
                                            </div>
                                            <% } %>
                                                <span>
                                                    <%= m.away %>
                                                </span>
                                </div>
                            </div>
                            <% }) %>
                                <% } %>
            </div>
        </main>

        <%- include('partials/footer') %>

            <script>
                function filterMatches(tag) {
                    document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
                    event.target.classList.add('active');

                    document.querySelectorAll('.schedule-card').forEach(row => {
                        row.style.display = (tag === 'all' || row.dataset.tag === tag) ? 'grid' : 'none';
                    });
                }
            </script>
</body>

</html>